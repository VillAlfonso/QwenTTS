# Qwen3-TTS Studio dependencies
#
# 1) Install CPU PyTorch first (Windows + AMD = CPU build):
#      pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# 2) Install the Qwen3-TTS engine (vendored in ./third_party):
#      pip install -e ./third_party/Qwen3-TTS
#    (this pulls transformers==4.57.3, accelerate==1.12.0, librosa, soundfile,
#     onnxruntime, einops, gradio, ...)
#
# 3) Web stack:
fastapi
uvicorn[standard]
python-multipart
huggingface_hub
hf_transfer

# Pinned engine deps (installed transitively by qwen-tts, listed for clarity):
# transformers==4.57.3
# accelerate==1.12.0
# torch, torchaudio (CPU)
# librosa, soundfile, numpy
